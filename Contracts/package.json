{
  "name": "credaura-contracts",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile && node scripts/copy-abi.js",
    "deploy:local": "npx hardhat run scripts/deploy.js --network hardhat"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
